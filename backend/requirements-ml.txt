# CLIP ViT-B/32 embeddings (EMBEDDING_BACKEND=clip or auto). Install CPU wheels with:
#   pip install -r backend/requirements-ml.txt --extra-index-url https://download.pytorch.org/whl/cpu
-r requirements.txt
torch>=2.3
transformers>=4.44
